# StarSeaKnow 开发协作规范

本文件适用于整个仓库。后端位于 `server/Spring-AI`，前端位于 `web`。

## 分支与 Superpowers 执行规则

- 开始功能开发或较大修改前先检查当前分支和工作区状态。
- 在 Superpowers 的执行阶段，如果当前位于 `main`，必须直接在当前仓库中创建 `codex/<简短主题>` 分支后再修改代码。
- 如果当前已位于对应的非 `main` 功能分支，则继续使用当前分支，不重复创建分支。
- 禁止为功能开发或 Superpowers 计划创建 Git worktree；统一在当前工作目录内通过普通分支完成修改。
- 创建或切换分支前必须保护现有未提交修改，不得覆盖、丢弃或混入其他人的工作。

## 提交规则

- 每项功能变更完成并验证后必须提交代码，不得让已完成的功能长期停留在未提交状态。
- 涉及多文件、公共接口、数据流、权限、架构或大范围界面的修改，必须在完成后提交代码。
- 提交前运行与改动范围相符的验证；验证失败时不得提交为完成状态，应先修复或明确报告阻塞原因。
- 每个提交只包含一个连贯主题，不得把无关修改、生成文件、密钥或其他人的临时改动混入提交。
- 提交信息使用 Conventional Commits 风格，采用 `feat:`、`fix:`、`refactor:`、`chore:`、`docs:` 等类型，并用简洁中文描述内容。
- 提交后检查 `git status --short`，确认没有遗漏本次应提交的文件。

## 基础验证

- 修改后端生产代码后，至少运行 `cd server/Spring-AI && mvn -q -DskipTests package`。
- 修改前端生产代码后，至少运行 `cd web && npm run build`。
- 如果仓库重新引入自动化测试，涉及相应模块的功能修改还必须运行对应测试套件。
- 构建或测试产生的警告需要评估；可能影响正确性、安全性或发布的警告必须在提交前处理。

## 仓库卫生

- 提交前使用 `git status --short` 和 `git diff --check` 检查变更。
- 不提交 IDE 配置、操作系统元数据、Agent 本地配置、依赖目录、构建输出、覆盖率文件、缓存、日志、临时文件、环境变量文件或本地数据库。
- 不得提交真实 API Key、访问令牌、密码、私钥或包含敏感数据的本地配置。
- 删除、覆盖或批量移动文件前必须确认精确目标；不得使用可能波及仓库根目录或用户目录的宽泛命令。
