<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.starsea.ai.mapper.FileProcessingMapper">

    <resultMap id="FileProcessingResultMap" type="com.starsea.ai.domain.FileProcessing">
        <id property="fileId" column="file_id" jdbcType="BIGINT"/>
        <result property="tenantId" column="tenant_id" jdbcType="BIGINT"/>
        <result property="knowledgeId" column="knowledge_id" jdbcType="BIGINT"/>
        <result property="pipelineState" column="pipeline_state" jdbcType="SMALLINT"/>
        <result property="failedFromState" column="failed_from_state" jdbcType="SMALLINT"/>
        <result property="progress" column="progress" jdbcType="SMALLINT"/>
        <result property="sourceHash" column="source_hash" jdbcType="VARCHAR"/>
        <result property="strategyCode" column="strategy_code" jdbcType="VARCHAR"/>
        <result property="plannerVersion" column="planner_version" jdbcType="VARCHAR"/>
        <result property="policySnapshot" column="policy_snapshot" typeHandler="com.starsea.ai.openapi.credential.PostgresJsonbTypeHandler"/>
        <result property="contextPolicy" column="context_policy" typeHandler="com.starsea.ai.openapi.credential.PostgresJsonbTypeHandler"/>
        <result property="lastError" column="last_error" jdbcType="LONGVARCHAR"/>
        <result property="lockVersion" column="lock_version" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <select id="findTimedOutAsync" resultMap="FileProcessingResultMap">
        SELECT *
        FROM file_processing
        WHERE pipeline_state IN (1, 5)
          AND update_time &lt; #{cutoff}
        ORDER BY update_time, file_id
    </select>

    <select id="findScopedForUpdate" resultMap="FileProcessingResultMap">
        SELECT *
        FROM file_processing
        WHERE file_id = #{fileId}
          AND tenant_id = #{tenantId}
          AND knowledge_id = #{knowledgeId}
        FOR UPDATE
    </select>

    <update id="transition">
        UPDATE file_processing
        SET pipeline_state = #{target},
            progress = #{progress},
            last_error = #{lastError},
            failed_from_state = #{failedFromState},
            lock_version = lock_version + 1,
            update_time = CURRENT_TIMESTAMP
        WHERE tenant_id = #{tenantId}
          AND knowledge_id = #{knowledgeId}
          AND file_id = #{fileId}
          AND pipeline_state = #{expected}
          AND lock_version = #{lockVersion}
    </update>
</mapper>
