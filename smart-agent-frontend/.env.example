# Development uses the Vite /api proxy (configured in vite.config.js).
VITE_API_BASE_URL=/api

# Override only when a deployment intentionally calls a backend origin directly.
# VITE_API_BASE_URL=https://api.example.com
